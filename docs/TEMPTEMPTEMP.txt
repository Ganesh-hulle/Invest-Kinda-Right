--START Spring-Boot
mvn clean spring-boot:run

--Completely ReSTART Docker
docker compose up -d

--Down Docker
docker compose stop

--Up Docker
docker compose start

--Kill Docker but keep volume
docker compose down

--Connect to POSTGRESQL
docker exec -it ikr-postgres psql -U ikr -d ikr 

--Connect to REDIS
docker exec -it ikr-redis redis-cli

adb -s RZCW40WBHTP reverse tcp:8080 tcp:8080

ssh -i .\ikr-production-key.pem ubuntu@16.16.236.149

--UBUNTU SETUP [DOCKER]
sudo apt update && sudo apt upgrade -y

sudo install -m 0755 -d /etc/apt/keyrings

curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

sudo chmod a+r /etc/apt/keyrings/docker.gpg

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update

sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

sudo usermod -aG docker $USER

newgrp docker

docker --version